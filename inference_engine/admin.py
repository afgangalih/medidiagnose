"""
inference_engine/admin.py
=========================
Django admin configuration for inference engine models.

Currently empty — inference engine logic is service-based, not model-based.
"""

from django.contrib import admin  # noqa: F401
